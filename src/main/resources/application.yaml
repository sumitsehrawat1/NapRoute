server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/naproute
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      platform: postgres
